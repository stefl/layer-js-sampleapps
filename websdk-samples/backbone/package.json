{
  "name": "layer-js-sampleapps-backbone",
  "version": "0.1.0",
  "description": "Backbone.js chat UI example using Layer SDK",
  "scripts": {
    "prepublish": "npm run build",
    "start": "http-server -a localhost",
    "build": "browserify app/index.js -o bundle.js",
    "watch": "watchify app/index.js -o bundle.js -v"
  },
  "dependencies": {
    "http-server": "~0.8.0"
  },
  "devDependencies": {
    "browserify": "~11.0.1",
    "underscore": "~1.8.3",
    "backbone": "~1.2.2",
    "jquery": "~2.1.4"
  }
}
